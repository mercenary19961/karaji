# Karaji — Project Context

> Quick reference for AI assistants and developers

> **📍 Doc sync:** CLAUDE.md last synced to commit `9fa1ce7` — 2026-07-11 10:33 (Sat). Convention: whenever you edit this file, refresh this line to the current commit — run `git log -1 --format="%h %cd" --date=format:"%Y-%m-%d %H:%M (%a)"` and paste the hash + date + time.

> **📌 Log the tricky stuff.** Whenever you hit an **issue, blocker, non-obvious behavior, or anything that cost real debugging time**, write it down with its **symptom → root cause → fix** — inline near the relevant section and/or a one-liner in the `> Last updated:` log. Same convention as Retab Stores / Sky Amman; the stack is shared, so a gotcha captured once saves every sibling project.

---

## Project Overview

**Working title:** Karaji — **Karaji** (en) / **كراجي** (ar) — ⚠️ **TEMPORARY name, rename pending.** Rule from day one: the brand name lives ONLY in **`config/brand.php`** (`name.ar`/`name.en`, shipped via the locale-aware shared `name` prop) plus `APP_NAME` for mail/CLI. **Never hardcode the name in UI strings, components, or copy** — the future rename must be an edit to those two lines plus a folder/repo rename.
**Product:** SaaS maintenance CRM for local car maintenance shops in **Amman, Jordan**. Records customers, cars, and service visits; its real value is a **reminders engine** that brings customers back to the shop (oil changes, license renewal, seasonal checks) via calls and WhatsApp.
**Portals:**
1. **Shop portal** — the garage owner/worker. **Arabic-only, RTL, mobile/tablet-first, radically simple** (users are NOT technical).
2. **Admin portal** — us (SaaS operator). English, LTR. Manages shops, subscriptions, support (impersonation), announcements; can view/edit everything with a full audit trail.

**Stack:** Laravel 12 + Inertia.js **v3** + React 19 + TypeScript + Tailwind CSS v4 (official `laravel/react-starter-kit` **v1.0.1** — PHP 8.2 pin, same as Retab Stores). Scaffold shipped Inertia v2.0.24; **upgraded to v3 on 2026-07-05** (`inertia-laravel` v3.1.1 + `@inertiajs/react` 3.6.0). The only break, same as Retab: v3 no longer auto-unwraps the page module's default export — `app.tsx` unwraps with `.then((m) => m.default)`, `ssr.jsx` returns `pages[...].default`.
**DB:** **Decided (2026-07-06): MariaDB in production, SQLite for local dev + CI** (`database/database.sqlite`; CI uses in-memory). Migrations stay engine-portable — no MariaDB-only column types without a portability check.
**Hosting:** TBD — likely Railway (FrankenPHP) behind Cloudflare, mirroring Sky Amman/Retab.
**Repo:** `origin` → `https://github.com/mercenary19961/karaji.git` (private). Single remote for now.
**Business model:** monthly subscription per shop in JOD (price TBD, ballpark 15–25 JOD/month, free first month). Admin portal controls activation/suspension.

---

## Reference Projects — read the implementation before building

Same machine, same stack, proven patterns. **When in doubt, read the corresponding file there first:**

- **`c:\Users\sabba\Desktop\projects\retab-stores\`** — closest and most recent. Port from here: **WhatsApp Meta Cloud API** integration (templates, webhooks, campaign sender), i18n/RTL foundation (`SetLocale`, `LanguageContext`, logical properties, Tajawal font token override), security hardening (SecurityHeaders, trustProxies, Turnstile, rate limits), media layer (`App\Support\Media`), SSR wiring (`TimeoutHttpGateway`), CI workflow (`.github/workflows/ci.yml` incl. the ziggy-js devDep gotcha), `withoutVite()` in TestCase.
- **`c:\Users\sabba\Desktop\projects\hardrock-ecom-demo\`** — activity log + undo (`ActivityLogService`), roles, notifications, optimistic locking.
- **`c:\Users\sabba\Desktop\projects\sky-amman\`** — locale middleware origin, admin layout, Site Content CRUD, security headers, SSR sidecar + Railway deploy notes. Also home of **`ChangeLogService`** (admin change log + revert + undo toast) — ⚠️ **before building Karaji's admin audit/undo, read sky-amman `CLAUDE.md` → "ChangeLog / Revert semantics — compound-edit behavior catalog"** (2026-07-06 audit): its full-snapshot reverts clobber newer edits to other fields. **Port the fixed v2 implementation from retab-stores instead** (`app/Services/ChangeLog/ChangeLogService.php` + `RevertResult.php` + `tests/Feature/Admin/ChangeLogTest.php`, built 2026-07-06): dirty-fields snapshots, per-field conflict check, transactional reverts, reverts as first-class entries (redo included).

**Don't blindly copy:**
- Retab's storefront is bilingual with an instant AR⇄EN toggle. Karaji's **shop portal is Arabic-ONLY in v1** (no toggle, no `_ar`/`_en` content columns for UI chrome) — the bilingual machinery is deliberately NOT needed here at first. Admin portal is EN.
- This starter kit uses lowercase `resources/js/pages/`, modular `routes/*.php`, shadcn/Radix `components/ui/` — same as Retab, different from Sky Amman.

---

## Product Spec (v1)

**Core insight: the reminders engine IS the product.** Record-keeping is the cost the shop pays; customers coming back is what they buy. If entering a visit takes over ~20 seconds, shops revert to the paper notebook and the product dies.

### Shop portal
- **Car-centric model:** a customer (phone) owns one or more **cars (plate number)**. One big search box: lookup by plate OR phone. The counter moment = type last digits of the plate → full history + what's due.
- **Visit entry, brutally minimal:** required = plate, current **km**, services done as **tappable chips** (تغيير زيت، فلتر زيت، فلتر هواء، فلتر مكيف، فحص فرامل، بطارية، دواليب…). Oil brand dropdown remembered from last visit; price and notes optional. New customer = phone + name, done.
- **Reminders by km AND date:** interval per oil type (mineral ~3,000–5,000 km, synthetic ~10,000 km, or 3–6 months). Estimate due-date from the car's average daily km across visits (shop-level default, e.g. 40 km/day, for first-visit cars).
- **Daily "call list":** cars due/overdue, sorted by overdue-ness, each with big **call** + **WhatsApp** buttons and a "تم التواصل ✓" state.
- **WhatsApp in two phases:** **Phase 1 = `wa.me` deep links** with pre-filled Arabic text (zero cost, no Meta approval, shop taps send). **Phase 2 (later, paid tier) = Meta Cloud API** automation with approved utility templates — reuse Retab's implementation.
- **Digital windshield sticker:** after each visit, one tap sends the customer a WhatsApp summary (what was done, km, next due km/date). Flagship demo feature.
- **Jordan-specific hooks:** annual **license renewal (الترخيص/الفحص)** month per car → reminder + pre-inspection offer; **seasonal broadcasts** (winter check ~Nov, AC ~May); Eid/Ramadan greeting broadcasts.
- **Dashboard:** today's visits, due-for-contact count, monthly revenue (if prices entered), "customers you're losing" (no visit in 8+ months). Analytics = 3–4 numbers + simple charts, no more.

### Admin portal
- Shops CRUD + **subscription status** (Active / Trial / Suspended), onboarding.
- **"Login as shop" impersonation** — support IS the product for non-technical users.
- Per-shop usage analytics (visits entered, reminders sent) → churn early-warning.
- Broadcast announcements to shop dashboards; notification system.
- Admin edits fully audited + undoable (port hardrock `ActivityLogService`).

### Deferred (v2+) — do NOT build in v1
Inventory/stock, multi-branch, employee roles beyond one shared shop login, appointments/booking, customer-facing portal, invoicing/tax, automated Cloud API sending (phase 2).

---

## UX Rules (MUST FOLLOW — shop portal)

The user is a garage owner, ~35–55, not technical, on a phone or cheap Android tablet, greasy hands, noisy shop. Every screen optimizes speed + obviousness over density.

1. **Bilingual shop portal (ar default = Ammani dialect, en optional), `dir` follows locale** (i18n added 2026-07-07). Arabic is Jordanian colloquial: `التلفون` not `الهاتف`, `الجاي` not `القادم`, `أهلين` not `مرحباً بك`, `شوف` not `عرض`, `ابعت` not `إرسال`, `قبل` not `منذ`, `مش` not `غير`, `زي` not `نفس`, `حكيت معه` not `تم التواصل`. **How to add strings:** UI chrome → add a key to BOTH langs in `resources/js/lib/i18n.ts` and use `useT()`; server-generated display strings → `App\Support\Format` (locale-aware) or `lang/{ar,en}/shop.php` via `__('shop.*')`. **Customer-facing WhatsApp templates STAY Arabic** (literal in components — they go to Arabic customers) regardless of UI language; the interpolated data uses the **`*Ar` prop variants** (`shop.nameAr`, `car.labelAr`/`ownerAr`, `reminder.dueAr`, `savedVisit.carLabelAr`/`ownerAr`) so a name/label never leaks the English form into an Arabic message. **DB-entered data** (car labels, customer names, service types, reminder labels) has an **optional `*_en` column** with a **locale-aware `displayX()` accessor that falls back to Arabic** — so a shop that enters only Arabic (the common case; the shop's own new-customer form has NO English input) still shows that Arabic value in the English UI (never blank). Only service types are genuinely bilingual (system vocabulary); the customer/car `_en` values are mostly seed-only demo polish. Admin portal is English. CSS **logical properties** only (`ms-*`/`me-*`/`ps-*`/`pe-*`, `text-start`, `ltr:`/`rtl:` variants) — never `left`/`right` — so both directions work.
   - **Topic separator = middle dot ` · ` (U+00B7), never an em-dash `—`.** Used for all "A · B" joins (owner · car, shop · area, owner · phone, car · owner · km). Zaid's global no-dashes rule applies to product copy too; `—` stays only in English code comments.
2. Mobile-first (~390px). Touch targets **≥48px**. Base font **≥16px**.
3. **Max 2 taps** from dashboard to any core action.
4. Icons **always paired with an Arabic label** — never icon-only.
5. Lists are **cards, not tables**. No horizontal scrolling, ever.
6. **No required field that can possibly be optional.** Visit entry ≤3 required fields.
7. **Undo instead of "are you sure?"** dialogs wherever feasible.
8. Numeric inputs (phone/plate/km/price) get `inputmode="numeric"` keypads.
9. **Never hardcode the brand name** (see Project Overview).

Admin portal: EN, LTR, denser UI allowed — but same component base (shadcn/Radix).

---

## Architecture Patterns (adopt from Retab/Sky Amman)

- **Inertia:** controllers `Inertia::render('page/name', [...props])`; mutations via `router.post/put/delete` with `preserveScroll`/`onSuccess`; files via `FormData` + `forceFormData`; flash via `->with('success', …)` → `usePage().props.flash`; auth via `usePage().props.auth.user`. No client-side router, no API routes for the UI.
- **Code quality:** dedicated model methods over raw `::update()`/`::create()`; one shared method per write path; **never query in a loop** (batch with `whereIn`/`pluck`); guard no-op writes; use returned values; match surrounding style.
- **Security (PORTED 2026-07-06, from Retab):** ✅ `SecurityHeaders` middleware in the `web` group (nosniff, SAMEORIGIN, Referrer-Policy, Permissions-Policy; +HSTS/CSP outside local — CSP skipped in local dev because Vite HMR's bracketed-IPv6 origin breaks Chrome; Cloudflare challenge/insights origins pre-allowed for future Turnstile/analytics). ✅ `trustProxies` locked to Cloudflare CIDRs + RFC 1918 (never `*`) — refresh from cloudflare.com/ips when CF updates. ✅ `URL::forceScheme('https')` in production. ✅ Rate limits: login/register 10/min, password flows 6/min, visit save+undo 30/min, search + reminder toggles 60/min, admin subscription writes 30/min, impersonation 10/min (generous on shop flows — busy counters tap fast). `Security\SecurityHardeningTest` covers headers + two 429 paths. **Still pending pre-launch:** Turnstile (needs keys + a decision on open registration vs admin-invited shops), upload validation (no uploads exist yet). Use `Auth::id()`/`Auth::user()`, never the `auth()` helper.
- **Multi-tenancy (DECIDED, schema v1):** single database, `shop_id` on all shop-owned tables, enforced by the **`App\Models\Concerns\BelongsToShop` trait** — a `'shop'` global scope that hard-filters to `Auth::user()->shop_id` when set, plus a `creating` hook that auto-fills `shop_id`. Rules: **admins have `shop_id = null`** and bypass the scope; **console/queue contexts are unscoped** (the reminders engine needs cross-shop reads); `shop_id` is **never `$fillable`** on tenant models (admin flows assign explicitly; factories bypass guards); bypass in queries only via explicit `withoutGlobalScope('shop')`. Applied to Customer, Car, Visit, Reminder. NOT applied to ServiceType (`shop_id` null = global default chips — scope would hide them), Subscription/Announcement/ActivityLog (admin-managed). Covered by `TenancyTest` (6 tests incl. cross-shop `find()` isolation).

---

## Local Development

- **Start:** `composer run dev` (serve + queue + vite) or `php artisan serve` + `npm run dev`. URL: `http://localhost:8000`.
- **DB:** SQLite at `database/database.sqlite` (scaffold default). Reset: `php artisan migrate:fresh`.
- **Tests:** `php artisan test` (PHPUnit; scaffold baseline = 26 passed / 63 assertions). **Build:** `npm run build`.
- **`.npmrc` holds `production=false` — do not delete.** The shell has a global `NODE_ENV=production` that otherwise makes `npm install` silently drop devDependencies. Fallback: `npm install --include=dev`.
- **Intelephense:** the machine-wide P1005/P1013 fix is already applied (porifa fork removed + `vendor/_laravel_ide` excluded — see retab-stores CLAUDE.md → Local Development for the forensic writeup). Per-project step once models exist: `composer require --dev barryvdh/laravel-ide-helper` + generate + gitignore the `_ide_helper*` files.

---

## Git & Commit Convention

- `origin` → `https://github.com/mercenary19961/karaji.git`. Commit/push **only when the user asks**. Branch off `main` for feature work.
- Format: `type(scope): short description` — types `init` · `feat` · `fix` · `refactor` · `style` · `doc` · `chore`; lowercase, imperative, no trailing period, ≲72 chars, specific.
- **No attribution trailer** — no `Co-Authored-By:` line, no "Generated with" line.
- After any task that touches code, end the reply with a **one-line suggested commit message** (don't run the commit).

---

## Build Progress

### Scaffold (DONE — 2026-07-05)
- [x] `laravel new karaji --react --phpunit --database=sqlite --no-interaction` — starter kit **v1.0.1** (PHP 8.2 pin), Laravel 12.62, Inertia **v2.0.24**, React 19, TS, Tailwind v4, Ziggy 2.6.3
- [x] `.npmrc` (`production=false`) added BEFORE `npm install`; deps installed with devDeps intact
- [x] Verified: `npm run build` ✓ (3.3s) + `php artisan test` ✓ (26 passed / 63 assertions)
- [x] Git init on `main`, `origin` → `mercenary19961/karaji` (private)
- [x] `APP_NAME=Karaji` in `.env` + `.env.example` (the ONLY place the working name lives)
- [x] This `CLAUDE.md` seeded (brief, UX rules, references, conventions)

### Foundation (TODO — rough order)
- [x] Init commit + push to `origin` (`d35024e`, 2026-07-05)
- [x] **Inertia v2 → v3 upgrade** (2026-07-05) — `inertia-laravel` v3.1.1 + `@inertiajs/react` 3.6.0; the default-export unwrap in `app.tsx`/`ssr.jsx` was the only change needed, exactly as Retab predicted. Verified: `npm run build:ssr` ✓ (client + SSR bundles) + `php artisan test` ✓ (26 passed / 63 assertions, scaffold baseline held)
- [x] **Arabic/RTL foundation** (2026-07-05) — `APP_LOCALE=ar` + `APP_FAKER_LOCALE=ar_JO` (Faker ships a Jordanian provider — Arabic names/addresses in seeds) in `.env`/`.env.example`, fallback stays `en`; `app.blade.php` gets Retab's dynamic `dir="{{ locale === 'ar' ? 'rtl' : 'ltr' }}"` + Tajawal in the Bunny fonts link; `app.css` gets the `html[dir='rtl']` `--font-sans` token override (Tajawal leads the stack under RTL only). **No toggle, no middleware** — locale is app-wide `ar`; when the admin portal exists, its route middleware sets `en` per-request and `lang`/`dir`/font all flip automatically. Verified: build + 26 tests green, served `/login` renders `<html lang="ar" dir="rtl">` with the Tajawal link
- [x] **HTML mockup** of the 7 core screens (2026-07-05) — designed in Claude Design, imported to `design/`. Two artifacts: `design/claude-design/mockup.dc.html` (+`support.js`) = the editable Claude Design source (round-trip future edits there), and **`design/mockup-v1.html`** = the flattened, fully self-contained static version (plain HTML + vanilla JS, zero external requests) — THE file to open/share. Shareable link (private, works on phone): https://claude.ai/code/artifact/cb5abd36-617a-46a6-8726-b91f001cadea . **Gotcha:** Claude Design exports are NOT self-contained — `support.js` loads React 18 + Babel from unpkg.com at runtime, so the `.dc.html` needs internet and dies under any strict CSP (Claude artifacts, future SecurityHeaders); flatten to static HTML before shipping a mockup anywhere. Design covers all spec beats: new-customer mini-form, post-save WhatsApp summary (windshield sticker), license-renewal + seasonal reminder cards, undo toast, wide LTR admin frame, light-only theme.
- [ ] **Validate mockup with a real shop owner** → collect reactions per screen (esp. visit-entry speed + WhatsApp summary) before any schema work — can now demo the REAL app (`/shop`) instead of the static mockup
- [x] **Shop portal demo screens** (2026-07-05) — mockup → real Inertia pages. Theme: mockup palette mapped onto the shadcn `:root` tokens + four new semantic pairs (`cta` amber action, `success`, `success-soft`, `due` badge) wired through `@theme`; `--radius` 1rem; `.dark` left stock (shop portal is light-only by design — garage sunlight). `APP_NAME="كراجي"` (brand rendered ONLY via the shared `name` prop in `shop-layout`). Backend: `routes/shop.php` behind `auth` (`/shop`, `/shop/visits/new`, `/shop/cars/demo`, `/shop/reminders`, `/shop/analytics`), `Shop\ShopScreensController` + `App\Support\ShopDemoData` — the demo arrays' shapes ARE the schema-v1 prop contract; swapping in Eloquent later touches only the controller. Frontend: `layouts/shop-layout.tsx` (header + 3-tab bottom nav, 48px+ targets), 5 pages under `pages/shop/`, types in `types/shop.ts`. **Live wa.me/tel: deep links** (phase-1 WhatsApp) with prefilled Arabic messages incl. the windshield-sticker visit summary built from actual form state. Client-side demo interactions only (no POSTs yet): chips, save→summary+undo toast, تم التواصل toggles, month picker driving the SVG chart. Tests 28 passed / 110 assertions (new `Shop\ShopPortalTest` renders all 5 screens), tsc + build clean. NOT built yet: any write path.
- [x] **Admin portal demo screens** (2026-07-05) — mockup screens 6-7 as real pages: `routes/admin.php` behind `auth` + `SetAdminLocale` (**TODO schema v1: admin-role gate — plain `auth` is demo-only**), `Admin\AdminScreensController` + `AdminDemoData`, `layouts/admin-layout.tsx` (EN/LTR, wide, denser), `admin/shops` (client-side search filter, status badges) + `admin/shop-detail` (stats, subscription card, "Login as shop →" linking to `/shop`, activity log with client-side Undo demo). **Gotcha (caught by test, would've bitten in prod):** `App::setLocale()` mutates process state — in worker-mode servers (FrankenPHP/Octane) and across requests within one test, a locale set for one request **leaks into the next**. Fix: `SetLocale` middleware appended to the `web` group pins `ar` on every request (hardcoded `'ar'`, NOT `config('app.locale')` — setLocale rewrites that key, so re-reading restores the leaked value); admin overrides to `en` afterwards via route middleware. `AdminPortalTest` asserts both portals' `lang`/`dir` in the same process. Tests 31 passed / 135 assertions.
- [ ] **Decisions:** real name/brand · hosting · subscription price — **decided:** DB = MariaDB prod / SQLite dev+CI (2026-07-06) · theme = light-only shop portal (2026-07-05) · **onboarding = admin-invited only, NO public signup (2026-07-07)** · palette + Jordanian-dialect copy (2026-07-07)
- [x] **Schema v1** (2026-07-06) — 11 migrations: shops, users +`role`/`shop_id`, subscriptions, customers, cars, service_types, visits, visit_services, reminders, announcements, activity_logs. Design notes: car `label` is free text (no forced make/model/year in v1), `license_month` 1-12 for الترخيص, customers unique per `(shop_id, phone)`, cars unique per `(shop_id, plate)`, reminders indexed `(shop_id, status, due_date)` for the call list, activity_logs carry JSON `changes` + `undone_at` for the undo pattern. Models with relationships + casts, `BelongsToShop` tenancy trait (see Architecture Patterns), factories for everything (ar_JO faker), `DatabaseSeeder` recreates the mockup demo shop exactly (**logins: admin@example.com / shop@example.com, password `password`**). ide-helper installed + configured per the machine checklist (mixins written, `_ide_helper*` gitignored, `ide-helper:generate` in post-update-cmd). Tests 37 passed / 147 assertions. NOT done yet: wiring the portal pages to these tables (next step), write paths, role gate on `/admin`.
- [x] **Shop portal wired to real data + first write paths** (2026-07-06) — `ShopScreensController`/`ShopDemoData` replaced by dedicated `Shop\{Dashboard,Car,Visit,Reminder,Analytics}Controller` (base `ShopController` holds `shopProps` + the last-oil-change line). **Live flows:** plate/phone fragment search (`cars/search`, `to=visit` variant lands on the visit form; misses flash an Arabic error), car profile from DB (visit timeline, license month via `Car::licenseMonthLabel()`, next-due banner from `pendingOilReminder`), **visit save** (`POST visits` — validates with Arabic messages, resolves car OR creates customer+car with phone-dedupe via `firstOrCreate`, attaches services, then schedules the pending oil reminder from the latest oil visit — **superseded 2026-07-06 by `ReminderEngine`; the old `Car::syncOilReminder` + fixed 5,000km/6mo constants are gone**), **undo** (`DELETE visits/{id}` re-derives the reminder from remaining history — undo over confirm ✓), reminders list with **تم التواصل toggle** (`markContacted`/`unmarkContacted`), analytics computed portably (PHP-side month buckets — no SQL date fns, works on SQLite AND MariaDB). `EnsureShopUser` middleware redirects admins to `/admin`. Flash shared via `HandleInertiaRequests` (`flash.success`/`flash.error`). Frontend: pages consume real props (`types/shop.ts` is the contract), new-visit is a real `useForm` POST with three entry states (found car / inline search / new-customer form). Tests: `VisitFlowTest` (13 tests — store+reminder math, reminder update-not-duplicate, new-customer path, phone reuse, undo re-derivation, validation, cross-shop 404s, search incl. tenancy, contact toggle) + reworked `ShopPortalTest`. **51 passed / 187 assertions.** Still demo-static: admin portal screens (AdminDemoData).
- [x] **Admin portal wired + role gate + impersonation + activity undo** (2026-07-06) — `AdminDemoData`/`AdminScreensController` replaced by `Admin\{Shops,Subscription,ActivityLog,Impersonation}Controller`. **Role gate:** `EnsureAdminUser` on the `/admin` group (shop users bounce to `/shop`; mirrors `EnsureShopUser`). **Real subscription controls:** plan change (auto-sets `price_jod` from `Subscription::PLANS`), suspend/activate, extend-trial — every change goes through `App\Support\AdminActivity::log()` (lean hardrock-style logger: who/what/before/after) and is **undoable once** from the activity list (`ActivityLog::undo()` restores the before-state, `undone_at` marks it spent; no-op updates write no log). **Impersonation ("Login as shop"):** `POST admin/shops/{shop}/impersonate` stores `impersonator_id` in session + logs in the shop's user; amber EN banner in the shop layout (shared `impersonating` prop) returns via `POST impersonation/leave` (route outside the admin group — the user is a shop user mid-impersonation). Admin flash strip in `admin-layout`. Seeder: +4 shops in varied subscription states. **⚠️ GOTCHA (cost real debugging):** a DB column named **`changes` collides with Eloquent's internal `Model::$changes`** property — outside the class `$model->changes` hits `__get` → attribute (works), but INSIDE model methods `$this->changes` reads the protected dirty-tracking property (empty array) → `isUndoable()` silently false. Symptom: undo no-ops while all data looks correct. Fix: renamed the column to `change_set`. Never name a column `changes` (same trap: `exists`, `wasRecentlyCreated`, `original`). Tests: `SubscriptionManagementTest` (6) + `ImpersonationTest` (4) + reworked `AdminPortalTest` (5). **63 passed / 250 assertions.**
- [x] **Change-log v2 ported from retab-stores** (2026-07-06) — the lean `AdminActivity`/`change_set` undo core replaced by `App\Services\ChangeLog\{ChangeLogService,RevertResult}` (see retab-stores for the origin + sky-amman CLAUDE.md for the failure catalog it fixes). `activity_logs` schema reworked in place (never deployed): `old_data`/`new_data` dirty-field snapshots, `label` (human text; `action` is now semantic created/updated/deleted), `reverts_log_id` self-link, `reverted_at`/`reverted_by`. Semantics: updates snapshot ONLY changed fields; revert conflict-checks per field against `new_data` and refuses with the diverged field list; every revert is transactional and returns an honest `RevertResult`; reverts write mirror entries (small "undo" chip in the UI) → **redo = revert the mirror**. Karaji v1 revertable matrix: `Subscription => [updated]` only — everything else audit-only until more admin CRUD exists. `shop_id` carried on every entry for the per-shop activity list. `ChangeLogTest` (8 compound-edit scenarios incl. per-field conflict isolation + redo). **72 passed / 281 assertions.**
- [x] **Security hardening ported from Retab** (2026-07-06) — see Architecture Patterns → Security for the full list. `SecurityHeaders` + CSP, locked `trustProxies`, `URL::forceScheme('https')`, rate limits across auth/shop/admin. `Security\SecurityHardeningTest`. **66 passed / 260 assertions.**
- [x] **Reminders engine v1** (2026-07-06) — the fixed `Car::syncOilReminder` constants replaced by `App\Services\Reminders\ReminderEngine` (the product's heart). **Oil:** `visits.oil_type` column (mineral/synthetic) drives per-type km+months intervals (`INTERVALS`: mineral 4,000km/4mo, synthetic 10,000km/6mo); due-DATE = whichever's sooner between the km interval at the car's **learned pace** (`averageDailyKm` = km delta ÷ days across its visit history, clamped 5-500, shop `default_daily_km` fallback for first-timers) and the time cap. Re-derived on visit save AND undo (`scheduleOilReminder`, single write path; replaces the old method). **License (الترخيص):** `generateLicenseReminders` creates a reminder within `LICENSE_LEAD_DAYS` (30) of each car's renewal month, idempotent, next-year occurrence auto-regenerates — run by `reminders:generate` command, scheduled `dailyAt('06:00')` in `routes/console.php`. Seasonal broadcasts deliberately NOT per-car (they belong to announcements/campaigns, not the call list). Frontend: new-visit form gets a mineral/synthetic chip pair (shown only when تغيير زيت is selected) + oil-brand relabeled "ماركة الزيت". Seeder: hero car's visits are synthetic so the engine schedules 92,500km (matches the mockup) instead of a hand-written reminder. `ReminderEngineTest` (7: per-type intervals, pace-based dates, fast-vs-slow, time-cap bound, license lead-window + idempotency + command). **79 passed / 292 assertions.**
- [x] **Responsive tablet/desktop redesign + modern palette** (2026-07-07) — approved from a preview artifact (I played the Claude-Design role directly; no round-trip needed for a refresh on an existing token system). **Palette refresh (both portals)** in `app.css :root`: deeper ink navy `#13324d` (was `#17456b`), punchier amber `#f59e0b` CTA, fresher emerald `#0f9d6e`, cool-slate neutrals (`#edf1f5` canvas, `#e2e8f0` borders, `#64748b` muted). Same navy/amber/green DNA, deeper + cleaner. **Responsive shop layout** (`shop-layout.tsx`): on **md+** a deep-ink **sidebar rail** (brand + nav incl. حسابي + shop identity + logout) replaces the mobile chrome; the mobile header + 3-tab bottom nav are `md:hidden` and **unchanged** (main capped `max-w-md` on phones, `md:max-w-6xl` open on tablet+). Each shop page got `md:` multi-column layouts (mobile identical): dashboard = greeting + inline search/CTA + stats row + `md:grid-cols-[1.6fr_1fr]` (due-list beside a **new desktop-only "زبائن ما رجعوا" side panel** — the spec's "customers you're losing", added to `DashboardController` as `lostCustomers`); reminders = `md:grid-cols-2 xl:grid-cols-3`; car = 2-pane (info+actions | timeline); analytics = chart+services 2-col; account/new-visit = centered `md:max-w-xl` forms. Verified green: tsc + build + eslint + pint + **100 tests**. ⚠️ **NOT visually verified in a browser** — no chromium-cli/Playwright installed and the app needs an auth session; correctness (types/build/tests) is proven but pixel/layout review is the user's on a real screen. Preview artifact: https://claude.ai/code/artifact/f31cacca-9df7-4786-b103-a51b4d985b6a
- [x] **Shop account page + logout + light-theme lock + desktop framing** (2026-07-06) — closed three gaps the running app exposed. **(1) Light-theme leak (bug):** the scaffold's `use-appearance` hook followed the OS dark preference, so on a dark-mode machine the whole app (incl. the light-only shop portal) rendered with the untuned `.dark` palette. Fixed: `applyTheme()` now only ever `remove('dark')` — **v1 is light-only app-wide** (the `.dark` tokens + appearance settings page remain but are inert; no dark design exists). **(2) No logout / account:** new `shop/account` page (Arabic, ShopLayout) — shows name/email/shop, a change-password form, and a **logout** button; reached via a `UserRound` icon button in the shop header. `Shop\AccountController` (`edit` + `updatePassword` with explicit `min:8` for Arabic messages, `current_password` check); routes `shop.account` + `PUT shop.account.password` (throttle 10/min). Logout reuses the existing `POST /logout`. `AccountTest` (6: render, password change happy/wrong-current/mismatch, logout, admin-gated). **(3) Desktop presentation:** the mobile-first `max-w-md` column looked lost on laptops — now wrapped in a `bg-muted` backdrop with the app as a centered card (`md:border-x md:shadow-2xl`). Deliberately NOT a bespoke wide-desktop layout: shop users are on phones/tablets, so that'd be low-value for v1. **100 passed / 370 assertions.**
- [x] **Auth cleanup: post-login routing + bilingual auth pages** (2026-07-06) — fixes two scaffold-leftover issues. **(1) Landing:** login/`/`/`/dashboard` all sent users to the starter-kit placeholder dashboard (the one with the GitHub-repo + Laravel-docs sidebar links). New `User::homeRoute()` (admin→`/admin`, shop→`/shop`) is the single source of truth; `AuthenticatedSessionController` redirects there, `/` redirects guests→login / users→their portal, `/dashboard` kept only as a redirect so stray `route('dashboard')` links (register/verify/confirm controllers still use it) land right. Scaffold `welcome`/`dashboard` pages now unreachable (left in place, harmless). **(2) Auth i18n:** the shared login/register/forgot/reset pages are **Arabic-first with an AR/EN toggle** (top-corner button in `auth-simple-layout`). Mechanism: `LocaleController` (`GET /locale/{ar|en}`, guest-accessible) parks the choice in the session; `SetLocale` now reads it (default `ar`). **Guard:** new `SetShopLocale` middleware forces `ar` on shop routes (mirror of `SetAdminLocale`'s `en`) so a toggled-EN session can't leak into the hardcoded-Arabic shop UI — the toggle only ever affects the shared auth pages. Locale shared via Inertia (`locale` prop → `SharedData`); each auth page carries an inline `{ar,en}` copy map (matches the project's no-i18n-library stance). RTL fixes: `ml-auto`→`ms-auto` (the jammed "forgot password"), `space-x-*`→`gap-*`. Tests: reworked `DashboardTest` (role redirects) + new `Auth\LocaleTest` (default ar, toggle to en, invalid ignored, shop stays ar); `AuthenticationTest` redirect assertion updated. **94 passed / 348 assertions.** NOTE: self-registration still creates a shopless shop-user (broken state) — tied to the pending open-registration-vs-invite decision; not fixed here. **RESOLVED 2026-07-07 (below): registration removed entirely, admin-invited only.**
- [x] **Bilingual shop portal (Arabic + English i18n)** (2026-07-07) — reverses the "Arabic-only shop" v1 decision (user chose all-screens proper i18n, both audiences). **Frontend:** `resources/js/lib/i18n.ts` = co-located `{ar,en}` dictionary + typed `useT()` hook reading the shared `locale` prop; all 7 shop screens + layout converted. **Backend:** `Format` (overdue/monthsAgo/monthName/price) is now locale-aware via `App::getLocale()`; validation/flash/search/oil-type/avatar strings moved to `lang/{ar,en}/shop.php` (`__('shop.*')`). **Persistence:** per-user `users.locale` column (migration `2026_07_07_000002`, fillable); `SetShopLocale` now sets `user->locale ?? 'ar'` (was force-`ar`); `LocaleController` persists the choice to the user (+ session) so it sticks across devices. **Toggle:** العربية/English switch in the account page. `dir`/`lang` flip via `app.blade.php` from the locale; font swaps via the `html[dir]` token; chevrons mirror with `ltr:-scale-x-100`. **Kept Arabic always:** customer WhatsApp templates + DB data. Tests: `LocaleTest` +3 (en-preference user sees en/ltr, toggle persists, guest session doesn't leak into shop). **106 tests.**
- [x] **Account profile picture (first file upload)** (2026-07-07) — shop users can add/replace/remove an avatar. `avatar_path` column on `users` (migration `2026_07_07_000001`); `User` exposes an appended **`avatar_url`** accessor (`Storage::disk('public')->url(...)`) and **hides `avatar_path`** so the raw path never ships to the frontend; `setAvatar()`/`removeAvatar()` use `forceFill` (not fillable) and delete the previous file. `Shop\AccountController@updateAvatar` validates **`image` (MIME) + `mimes:jpg,jpeg,png,webp` (extension) + max:4096** (the extension-AND-MIME rule), stored on the `public` disk under `avatars/`; `@deleteAvatar` clears it. Routes `POST/DELETE shop/account/avatar` (throttle 20/min). Frontend: account page has an avatar + غيّر الصورة (hidden file input, `router.post` `forceFormData`) / شيل الصورة, and the avatar shows in the sidebar footer + mobile header account button (from shared `auth.user.avatar_url`). **Needs `php artisan storage:link`** (done locally; add to prod deploy). `AccountTest` +5 (upload, non-image rejected, replace-deletes-old, remove, url-exposed-not-path). **104 tests.** ⚠️ Avatars serve from same-origin `/storage/...` so the current CSP (`img-src 'self' data: blob:`) is fine; **if avatars move to R2/S3/CDN later, add that host (or `https:`) to `img-src` in `SecurityHeaders`.**
- [x] **Registration removed — admin-invited onboarding only** (2026-07-07) — resolves the open-registration-vs-invite decision. Deleted the register route (`routes/auth.php`), `RegisteredUserController`, `pages/auth/register.tsx`, `RegistrationTest`, and the dead scaffold `pages/welcome.tsx` (both linked `route('register')`, now gone from Ziggy). `/register` 404s (asserted in `AuthenticationTest::test_public_registration_is_disabled`). Login page's "Sign up" replaced with a bilingual **"تواصل معنا واتساب / Contact us on WhatsApp"** link → `wa.me/{config('contact.whatsapp')}` (new `config/contact.php` + `CONTACT_WHATSAPP` env, placeholder `962790000000` — **set the real operator number before launch**). Shops are created by admins. **99 tests.** Follow-on: Turnstile is now only relevant to login/password-reset (no public signup form to protect), lowering its urgency.
- [x] **Custom dropdown (styled Radix Select) replaces native `<select>`** (2026-07-07) — rebuilt `components/ui/select.tsx` (the existing shadcn Select was default-styled + physical-property = broke RTL) into the app's design: rounded-xl `border-2` trigger with app tokens, a rotating chevron, touch-friendly rows (`py-3`), a check indicator, and a panel whose **width is locked to the trigger** (`w-[var(--radix-select-trigger-width)]`) so it always fits the mobile column. **Fully RTL-safe** via logical properties (`ps/pe`, `start-*`) — and because the panel width equals the trigger's, alignment lines up even though Radix's internal `dir` defaults to LTR (no `DirectionProvider` needed). Used everywhere a select is needed (build accordingly): shop new-visit **oil brand**, admin **announcement audience**, admin **subscription plan**. ⚠️ **Radix gotcha:** `SelectItem` value can't be `''` (reserved) — the announcement "All shops (broadcast)" uses a `'broadcast'` sentinel mapped to/from `''` in the handler. 139 tests green.
- [x] **Password show/hide toggle** (2026-07-07) — reusable `components/password-input.tsx` (wraps the shadcn `Input`, eye/eye-off button at the logical `end-0`, `tabIndex={-1}` so it stays out of the type→submit flow, aria-label localized ar/en from the shared `locale`). Applied to every password field: `auth/{login,reset-password,confirm-password}`, `settings/password`, `delete-user`, and `shop/account` (change-password ×3 — passes the shop's field classes + `ring-0` overrides to keep the border-colour focus look). No test/gate changes (139 tests still green).
- [x] **New-visit entry page: instant search + recents + new-customer shortcut** (2026-07-07) — a dedicated landing for starting a visit (`shop/entry`, `Shop\EntryController`), reachable from a **prominent amber "New visit" CTA at the top of the desktop sidebar** and the dashboard's big CTA (both now point at `shop.entry`; the mobile bottom nav stays 4 tabs). **Instant search is fully client-side:** the controller ships the shop's whole car index (`id, plate, label, owner, phone, lastVisit`, tenancy-scoped, newest-visit first, capped at 500) and the page filters as-you-type with **zero round-trips** — a `scoreCar()` ranker so a numeric query hits plate/phone (leading match > contains) and text hits owner/label. Below the (empty) search: **recent customers** (top 6 of the recency-sorted index) as tap-to-open cards → the visit form. **New-customer action** is a distinguished amber button: a **floating pill above the bottom nav on mobile** (`fixed bottom-24`), an inline button on desktop, plus a fallback in the no-results state → `visits/new?new=1`, which opens the form straight in new-customer mode (`VisitController` passes `startNew`; `new-visit.tsx` seeds `useState(startNew)`). The old inline search on `new-visit` stays as a harmless fallback. Sidebar active-state recolored (active items = subtle `bg-white/15`; amber is now reserved for the New-visit CTA). Tests: `Shop\EntryTest` (4: index shape, tenancy scope, recency order, `startNew`) + `ShopPortalTest` renders `shop/entry`. **139 passed / 711 assertions.**
- [x] **Bilingual seeded shop data: cars, customers, service types, reminders** (2026-07-07) — extended the bilingual layer to the per-record content in the "to contact today" / reminders / car / analytics screens (was still Arabic in the English UI). Migration `2026_07_07_000008` adds `service_types.name_en`, `customers.name_en`, `cars.label_en`, `reminders.label_en` (all nullable). Each model got a locale-aware **`displayX()`** accessor with **Arabic fallback** (`Customer::displayName`, `Car::displayLabel`, `ServiceType::displayName`, `Reminder::displayLabel`) — so a shop that enters only Arabic (its new-customer form has no English field) still shows the Arabic value in English (never blank; confirmed by the user's own rule + a test). `ReminderEngine` sets `label_en` on oil (`ServiceType::OIL_CHANGE_EN`) and license reminders; `ShopController::lastOilLine` is now locale-aware. **WhatsApp-safety:** the customer-facing messages are Arabic, so the controllers pass **`*Ar` variants** (`car.labelAr`/`ownerAr`, `reminder.carAr`/`ownerAr`/`dueAr`, `savedVisit.carLabelAr`/`ownerAr`) that the frontend WA builders use instead of the (possibly-English) display fields — mirrors `shop.nameAr`. Service-type chips: the prop now carries `name` (stable Arabic key for form matching + the Arabic WA summary) **and** `label` (localized caption); the new-visit `s.name === 'تغيير زيت'` matching is unaffected. Seeder fills English for all demo cars/customers/services/reminders (hero + call list + lost). Tests: `Shop\LocaleContentTest` +4 (reminder/car/owner per locale with Arabic WA fields, **Arabic-only fallback**, service chip label vs key). **135 passed / 648 assertions.**
- [x] **Bilingual DB content: brand name, shop name/area, announcements + seasonal templates** (2026-07-07) — extended the shop-portal i18n from UI chrome to the *data* that renders in it. **(1) Brand name** is now locale-aware (**Karaji** / **كراجي**): lives in **`config/brand.php`** (`name.ar`/`name.en`), shipped via the shared `name` prop. **⚠️ GOTCHA (caught by test):** `HandleInertiaRequests::share()` runs in the **web group, BEFORE** the route middleware (`SetShopLocale`/`SetAdminLocale`) applies the per-user locale — so a *direct* `config('brand.name.'.app()->getLocale())` value captured 'ar' too early (while the controller-computed props were already 'en'). Fix: **locale-dependent shared props (`name`, `locale`) must be closures** (`fn () => …`), which Inertia resolves at render time, after that middleware — same reason `flash`/`shopUnread` are closures. This also fixed a latent bug where the shared `locale` prop could disagree with the page for a fresh English-preference user (no session locale yet). **(2) Shop `name`/`area`** got `name_en`/`area_en` columns (`2026_07_07_000006`); `Shop::displayName()`/`displayArea()` pick per `App::getLocale()` (English falls back to Arabic). `ShopController::shopProps` returns `name`/`area` (display) **plus `nameAr`** (always Arabic) — the customer-facing WhatsApp templates in car/analytics/new-visit use `shop.nameAr` so they stay Arabic regardless of UI language. Admin can edit all four fields from a new "Shop details" form on `admin/shop-detail` (`PUT admin/shops/{shop}`). **(3) Announcements** got `title_en`/`body_en` (`2026_07_07_000007`); `displayTitle()`/`displayBody()` locale-aware with Arabic fallback; the shop dashboard banner + admin publish form are bilingual (paired ar/en inputs). **(4) Seasonal templates:** `config/announcement_templates.php` = prewritten bilingual announcements (winter check, summer A/C, Ramadan, Eid, license season); the admin announcements page has a "Start from a template" picker that pre-fills both languages (still editable). Seeder sets English name/area on all shops + English copy on the winter broadcast. Tests: `Shop\LocaleContentTest` (6: brand+shop+announcement per locale, both fallbacks) + `Admin\ShopUpdateTest` (3) + `Admin\AnnouncementTest` +2 (bilingual publish, templates prop shape). **132 passed / 608 assertions.**
- [x] **RTL/LTR follows the language toggle + dismissible announcements** (2026-07-07) — two fixes surfaced by the running bilingual portal. **(1) Direction stayed stale on toggle (bug):** `app.blade.php` sets `<html lang/dir>` from `app()->getLocale()`, but that only runs on the initial full page load — the AR/EN toggle is a client-side Inertia visit that swaps the page component and never re-renders `<html>`, so switching to English kept the RTL layout (English text laid out right-to-left) until a hard refresh. **First attempt used `router.on('navigate')` and did NOT work** (event didn't fire on the redirect-visit / stale build) — so the fix drives it from React instead: `app.tsx` wraps the page via the `<App>` children render-prop in a tiny `LocaleDirection` component whose `useLayoutEffect([locale])` sets `document.documentElement.lang`/`dir`. **Why this is airtight where the event wasn't:** the text switching to English already proves `<App>` re-rendered with `locale:'en'` merged props, and this wrapper sits in that exact prop flow (same source `useT()` reads) — it can't miss a change an event listener can. Safe because the kit has **no `Component.layout` persistent layouts** (grepped) so the render-prop override loses nothing, and the wrapper is a fragment (no DOM/hydration change). This alone fixes everything downstream: Tailwind `rtl:`/`ltr:` variants re-evaluate off `[dir]`, logical properties already worked, and the `html[dir='rtl']` Tajawal font token swaps. **Lesson:** for anything that must track the shared locale/props on client visits, drive it from a React effect on the props, not a `router` event. **(2) Announcement banners are now dismissible per-shop:** new `announcement_dismissals` pivot (`2026_07_07_000005`, unique `(announcement_id, shop_id)`); `Announcement::dismissedBy()` belongsToMany + `scopeActiveForShop` gains `whereDoesntHave('dismissedBy', shop)` so a dismissed banner disappears — **for that shop only** (a broadcast stays live for everyone else; the admin's record is untouched). `Shop\AnnouncementController@dismiss` (`POST shop/announcements/{announcement}/dismiss`, throttle 30/min) records the dismissal via `syncWithoutDetaching` (idempotent). Frontend: an `X` dismiss button on the dashboard banner (`router.post` + `preserveScroll`); persists across the shop's devices (shared login). `DashboardAnnouncementTest` +3 (dismiss hides + sticks, per-shop isolation, double-dismiss no-op). **121 passed / 493 assertions.**
- [x] **Two-way messaging: admin↔shop inbox + suggestions** (2026-07-07) — the direct, per-shop channel that complements broadcast Announcements (announcements = dashboard alerts, no read-tracking; messages = a targeted inbox with unread state). **Two tables** (`2026_07_07_000003/000004`): `messages` (admin→one shop, `read_at` for unread; **NOT `BelongsToShop`** — admin creates them for arbitrary shops with an explicit `shop_id`, so the auto-fill/scope would fight the flow; shop queries filter by `shop_id` explicitly) and `suggestions` (shop→admin, **uses `BelongsToShop`** so a shop user's create auto-fills `shop_id` and only sees its own; admin `shop_id=null` sees all; `status` open/reviewed). **Shop side:** `Shop\InboxController` renders `shop/messages` — messages list (opening the inbox marks all read via a single bulk `update`, but the already-fetched collection keeps the pre-open `unread` flags so the "new" styling still shows once) + a suggestions composer (`POST shop/suggestions`, throttle 20/min, body required) + the shop's own suggestions with open/reviewed chips. New **4th bottom-nav tab** (`grid-cols-3`→`4`) + sidebar item "الرسائل/Messages" with an **unread badge** from a shared `shopUnread` prop (computed lazily in `HandleInertiaRequests`, guarded to shop users only — no query on admin/guest). **Admin side:** compose form + sent-messages list (read/unread badges) on `admin/shop-detail` (`POST admin/shops/{shop}/messages`); new **`admin/suggestions`** page + nav item lists every shop's suggestions (open-first) with a mark-reviewed/reopen toggle (`PUT admin/suggestions/{suggestion}`). i18n: `msg.*`/`nav.messages` keys in `lib/i18n.ts` + `suggestion_*` in `lang/{ar,en}/shop.php` (admin UI stays literal EN). Seeder adds 2 demo messages (1 unread) + 1 suggestion to the demo shop. Tests: `Shop\InboxTest` (7) + `Admin\MessagingTest` (5). **118 passed / 462 assertions.**
- [x] **Language toggle moved into the shop chrome** (2026-07-07) — the العربية/English switch (was only on the account page) now lives where it's reachable in one tap: a **sidebar button** (above logout) on md+ and a compact **`ع`/`EN` header button** on mobile, both `router.get(route('locale', otherLocale))`. Removed the account-page language card. Same `LocaleController` persistence (session + `users.locale`).
- [x] **Announcements** (2026-07-06) — the last unbuilt spec item; the `announcements` table/model existed from schema v1. `Announcement::scopeActiveForShop($shopId)` = is_active + (broadcast OR targeted) + within the optional start/end date window. **Admin** (`Admin\AnnouncementController`, new "Announcements" nav item in `admin-layout`): publish (audience = All shops broadcast / one shop; optional date window with `after_or_equal` validation), pause/activate toggle, delete — page `admin/announcements` (create form + list). **Shop:** `DashboardController` injects `activeForShop` results; amber `Megaphone` banner cards at the top of the shop dashboard (reuses the `due` token). This IS the v1 "notification system" (dashboard alerts) — no per-user read-tracking in v1 (deferred). Seeder adds a live winter-check broadcast. Tests: `Admin\AnnouncementTest` (5: broadcast/targeted publish, date validation, toggle+delete, shop-user gated) + `Shop\DashboardAnnouncementTest` (4: broadcast+own visible, other-shop hidden, inactive/expired hidden, future-dated hidden). **88 passed / 332 assertions.**
- [x] **CI** (2026-07-05) — Retab's workflow ported: `.github/workflows/ci.yml` with two jobs — `backend` (PHP 8.2 + sqlite3/gd, composer install, `cp .env.example .env` + key:generate, `php artisan test` on in-memory SQLite) and `frontend` (node 22, `npm ci`, `tsc --noEmit`, client + SSR builds). Enablers: `withoutVite()` in `tests/TestCase.php` (page-render tests must not depend on the gitignored `public/build` manifest) and **`ziggy-js: "2.6.3"` pinned in devDependencies** — the `ziggy-js` import in `app.tsx` is type-only (esbuild drops it; Vite never resolves it) and tsc resolves it locally via the tsconfig path into `vendor/tightenco/ziggy`, so the composer-less frontend job needs the npm package (which ships its own `index.d.ts`) as the fallback. All four CI steps verified locally before push. **Follow-up (same day):** the scaffold also shipped stock `lint.yml`/`tests.yml` workflows — the `linter/quality` job failed on push because `eslint .` linted `design/claude-design/support.js` (imported generated runtime). Fixes: `design` added to `eslint.config.js` ignores; all three workflows consolidated into the single `ci.yml` with a new check-only `quality` job (`pint --test` + `prettier --check` + `eslint .`, PHP 8.2 — the stock job used mutating fixers whose output CI discarded, on PHP 8.4 against our 8.2 pin); stock `lint.yml`/`tests.yml` deleted. First full format pass applied: prettier normalized 13 frontend files (Tailwind class order), pint fixed 10 stock scaffold PHP files (the kit does NOT ship pint-clean).
- [x] **Analytics reports: month picker + chart redesign** (2026-07-11) — the التقارير screen got a real month/year **calendar picker** (tap the date → a Radix `Dialog` with a year stepper + a 3×4 month grid; each cell shows the month **number** under its name, future months disabled). Navigation is now **server-driven**: the arrows AND the calendar pick a month via an Inertia partial reload (`only: ['analytics']`), and the 6-bar chart **slides so the selected month is always the rightmost/highlighted bar** (browse full history, not a fixed last-6). `AnalyticsController` accepts `?month=YYYY-M` (clamped: never future, never absurdly old), and `topServices` re-scopes to the browsed 6-month window (`lostCustomers` stays present-tense = "call now"). **Chart redesign:** faint full-height "track" columns so an empty month reads as a real `0` (not a floating number), a value label above **every** bar, a **numeric x-axis** (the long Levantine names — كانون الثاني/تشرين الأول — collided as axis labels), and a `أيار · 0` readout in the card header. `Shop\AnalyticsTest` (5). Committed `9fa1ce7`.
- [x] **Per-service pricing + revenue** (2026-07-11) — replaced the single per-visit `price` with **per-service prices** so التقارير "top services" shows revenue (the money the shop makes per service). Schema: new **`service_prices`** table (per-shop default charge per service, `BelongsToShop`), a **`price` column on the `visit_services` pivot** (the amount charged, snapshot at save time — editable per visit), and **dropped `visits.price`** (retired, never deployed — the pivot is the single source of truth; `Visit::revenue()` = sum of pivot prices, null when unpriced so callers hide the line). **Settings (the "profile"):** new `shop/service-prices` page (`ServicePriceController` edit/update, reached from the account page + a desktop-sidebar "أسعار الخدمات" `Coins` item — mobile reaches it via account) where the shop sets a default price per service; a cleared box deletes the default. **Visit form:** each selected service shows a small price box **pre-filled from the shop default** (still editable) + a live total; on save each service's price = the per-visit override, else the shop default, else null. **Revenue reads from the pivot everywhere:** dashboard `monthRevenue` + car-timeline per-visit total (computed in PHP for SQLite/MariaDB portability), analytics top-services (`ServiceType::withSum(['visits as revenue' => $closure], 'visit_services.price')`). ⚠️ **Gotcha/pattern:** `withSum` works over a **belongsToMany pivot column** (`visit_services.price`) with an aliased constrained relation — reusable. Decimal pivot values read back loosely on SQLite (numeric affinity) → tests assert with `assertEquals`, not `assertSame`. Seeder: 6 default prices + per-service pivot prices (a couple overridden to demo the override) → demo shop = 210 JOD. Tests: `Shop\ServicePriceTest` (5) + `VisitFlowTest` pricing (+2) + `AnalyticsTest` revenue (+1). **150 passed / 795 assertions.** Not browser-verified (no headless browser).

> **Last updated:** 2026-07-11 — **Per-service pricing + revenue**: replaced the single per-visit price with per-service prices so analytics "top services" shows revenue. New per-shop **`service_prices`** defaults table + a **`price` column on the `visit_services` pivot**; **`visits.price` dropped** (`Visit::revenue()` = pivot sum, the single source of truth). New `shop/service-prices` settings page (default charge per service, reached from the account page + a desktop-sidebar item), the visit form's service chips each get a price box **pre-filled from the shop default** (editable, with a live total), and revenue now flows to analytics top-services + dashboard + car timeline. ⚠️ `withSum(['visits as revenue' => $closure], 'visit_services.price')` sums a **belongsToMany pivot column** — reusable; decimal pivot reads back loosely on SQLite (assert with `assertEquals`). 150 tests green (not browser-verified). Earlier: **Analytics month picker + chart redesign**: the التقارير screen got a month/year **calendar picker** (tap the date), **server-driven** month navigation (arrows + calendar reload via `only:['analytics']`; the 6-bar chart slides so the selected month is the rightmost bar → full history), and a chart refresh — full-height "track" columns so an empty month reads as a real `0`, value labels on every bar, a **numeric x-axis** (the long Arabic month names collided), and deep-green visit-count numbers to distinguish them from the gray/navy month numbers. Committed `9fa1ce7`. Earlier: **Custom dropdown**: rebuilt `ui/select.tsx` (styled Radix Select) into the app design — rounded-xl, RTL-safe (logical properties), touch-friendly rows, panel width locked to the trigger so it fits the mobile column; replaced all native `<select>` (oil brand, announcement audience, subscription plan). Reusable everywhere. Gotcha: Radix `SelectItem` can't have an empty-string value (broadcast uses a sentinel). 139 tests green. Earlier: **Password show/hide toggle**: reusable `PasswordInput` (eye button, RTL-safe, localized aria-label) on every password field (login, reset, confirm, settings, delete-account, shop change-password). 139 tests green. Earlier: **New-visit entry page**: a dedicated landing (`shop/entry`) to start a visit — instant, fully client-side typeahead over the shop's car index (numeric → plate/phone, text → owner/label, zero round-trips), recent-customer shortcuts, and a distinguished new-customer action (floating pill above the mobile bottom nav / inline on desktop → `visits/new?new=1` opens the form in new-customer mode). Added a prominent amber "New visit" CTA to the top of the desktop sidebar (dashboard CTA + sidebar both point here). 139 tests green. Earlier: **Bilingual seeded data**: extended the English layer to per-record content (car labels, customer names, service types, reminder labels) via nullable `*_en` columns + locale-aware `displayX()` accessors that **fall back to Arabic** when a shop entered Arabic only (its customer form has no English input); customer WhatsApp messages use new `*Ar` prop variants so they stay Arabic even in the English UI. Only service types are truly bilingual; the rest is seed polish. 135 tests green. Earlier: **Bilingual DB content**: the brand name (now **Karaji**/**كراجي**, in `config/brand.php`), each shop's name/area, and admin announcements all follow the shop user's UI locale (English falls back to Arabic); added prewritten bilingual **seasonal announcement templates** (`config/announcement_templates.php`) with an admin picker, an admin "Shop details" edit form for the four name/area fields, and `shop.nameAr` so customer WhatsApp templates stay Arabic. ⚠️ Gotcha logged: locale-dependent **shared Inertia props must be closures** — `share()` runs before the per-user-locale route middleware, so a direct value captures 'ar' too early (also fixed a latent shared-`locale` mismatch for fresh English users). 132 tests green. Earlier: **RTL follows the toggle + dismissible announcements**: fixed a bug where switching AR⇄EN in the shop portal changed the text but not the direction (English rendered RTL) — `<html lang/dir>` is Blade-set on first load only, so `app.tsx` now re-syncs `document.documentElement` from the shared `locale` prop via a React `useLayoutEffect` wrapper (`LocaleDirection` in the `<App>` render-prop — a `router.on('navigate')` listener was tried first and didn't fire reliably), which also re-triggers Tailwind `rtl:`/`ltr:` variants and the Tajawal font swap. Also made the admin announcement banners **dismissible per-shop** (new `announcement_dismissals` pivot + `whereDoesntHave` in `scopeActiveForShop`; an `X` on the dashboard banner posts to `shop.announcements.dismiss`) — hiding sticks across the shop's devices and only affects that shop, never the admin's broadcast. 121 tests green. Earlier: **Two-way messaging + language toggle relocation**: added a direct admin↔shop channel alongside broadcast Announcements — `messages` (admin→one shop, unread `read_at`, not tenant-scoped) surface in a new shop **inbox** (4th bottom-nav tab + sidebar item with an unread badge from a shared `shopUnread` prop) that marks-read on open; `suggestions` (shop→admin, tenant-scoped) let a shop request features ("add a brake-fluid change service") from the same page, and admins triage them open→reviewed on a new `admin/suggestions` page + compose messages from `admin/shop-detail`. Also moved the AR/EN toggle out of the account page into the shop chrome (sidebar button on desktop, `ع`/`EN` header button on mobile). 118 tests green. Earlier: **Bilingual shop portal** (Arabic + English): reversed the Arabic-only decision; added a real i18n layer (`lib/i18n.ts` + `useT`, locale-aware `Format`, `lang/{ar,en}/shop.php`), a per-user `locale` preference (`SetShopLocale` reads it), and an العربية/English toggle in the account page; customer WhatsApp messages + DB data stay Arabic. 106 tests green. See UX Rule #1. Earlier: **Account profile pictures** (first file upload): shop users add/replace/remove an avatar (validated image+MIME, `public` disk, `avatar_url` accessor exposed / raw path hidden), shown on the account page + sidebar + mobile header; needs `storage:link` on deploy; CSP note if storage goes remote. 104 tests. Earlier: **Registration removed (admin-invited only)**: deleted the signup route/controller/page + dead welcome page; login now shows a WhatsApp "contact us" link (`config/contact.php` + `CONTACT_WHATSAPP`, placeholder number — set real one pre-launch); `/register` 404s. Resolves the onboarding decision. 99 tests green. Earlier: **Jordanian dialect + middle-dot separator**: shifted all shop-portal copy from MSA to Ammani colloquial (`التلفون`/`الجاي`/`أهلين`/`شوف`/`ابعت`/`قبل`/`مش`/`حكيت معه`… incl. WhatsApp templates) and replaced every `—` topic separator with ` · `; greeting now counts `زبائن` not `سيارة` (per Zaid). See UX Rules #1. 100 tests green, re-seeded. Earlier: **Responsive redesign + modern palette**: refreshed both portals to a deeper-ink / punchier-amber / cool-slate palette, and gave the shop portal a real tablet/desktop layout (deep-ink sidebar rail + multi-column canvas) while keeping mobile identical; added the spec's "customers you're losing" panel to the dashboard. Green on tsc/build/lint/pint/100 tests; not browser-verified (no headless browser installed — user reviews visually). Earlier: **Shop account + logout + light-theme lock**: fixed a dark-mode leak (v1 is now light-only app-wide via `use-appearance`), added an Arabic shop account page (change password + logout, reached from a header icon), and framed the mobile-first shop portal as a centered card on laptops (muted backdrop). 100 tests green. Earlier: **Auth cleanup**: login now lands users on their real portal (was the starter-kit placeholder dashboard with repo/docs links); the shared auth pages (login/register/forgot/reset) are Arabic-first with an AR/EN toggle, guarded so the toggle never leaks into the Arabic-only shop portal (`SetShopLocale`); RTL layout bugs fixed. 94 tests green. Earlier: **Announcements** (last v1 spec item): admin publishes broadcast or shop-targeted announcements with an optional date window; they surface as amber banner cards on the shop dashboard (`Announcement::scopeActiveForShop`). This is the v1 notification system; per-user read-tracking deferred. 88 tests green. Earlier: **Reminders engine v1** (the product's heart): per-oil-type intervals (mineral/synthetic), due-dates from each car's learned average daily km with the shop 40km/day fallback + a time cap, oil reminders self-derive on visit save/undo (`ReminderEngine::scheduleOilReminder`), daily `reminders:generate` command creates license-renewal reminders in a 30-day lead window; 79 tests green. Earlier: **Change-log v2 ported from retab-stores** (dirty-field snapshots, per-field conflict-checked transactional reverts, mirror entries = redo; Subscription updates revertable, rest audit-only; 72 tests green). Earlier: **Security hardening ported from Retab** (SecurityHeaders + CSP, locked trustProxies, https scheme, rate limits across auth/shop/admin; 66 tests green; Turnstile + open-registration decision still pending pre-launch — see Architecture Patterns → Security). Earlier: **Admin portal wired**: role gate, real subscription controls with activity-log undo, working "Login as shop" impersonation with return banner; gotcha logged (never name a DB column `changes` — collides with Eloquent internals, see Build Progress). 63 tests green. Earlier same day: **Shop portal is now a real app**: all 5 screens query the DB, visits save/undo for real, oil reminders self-derive (`Car::syncOilReminder`), search works, تم التواصل persists. 51 tests green. Earlier same day: **Schema v1 + tenancy + demo seeder** (11 migrations, `BelongsToShop` scoping documented under Architecture Patterns, seeder mirrors the mockup, ide-helper wired, 37 tests green; DB decided: MariaDB prod / SQLite dev). Earlier (2026-07-05): **CI ported from Retab** (two-job workflow, `withoutVite()` TestCase, ziggy-js devDep pin — see Build Progress; all steps verified locally). Earlier: **Admin portal demo screens built** (mockup 6-7 → real pages; locale-leak gotcha found by test and fixed with a per-request `SetLocale` web middleware — see Build Progress; 31 tests green). Earlier: **Shop portal demo screens built** (mockup → real Inertia pages, demo props as schema contract, live wa.me links, 28 tests green; see Build Progress). Earlier: **Mockup v1 imported + flattened.** 7-screen mockup built in Claude Design, imported into `design/` (dc source + self-contained `mockup-v1.html`), published as a private artifact for phone viewing. Gotcha logged: Claude Design exports depend on unpkg CDN at runtime — flatten before sharing/CSP. Also fixed the scaffold's latent type error (starter kit v1.0.1 `welcome.tsx` ships `mixBlendMode: 'plus-darker'`, absent from csstype's union — surfaces the first time anyone runs `tsc`, since Vite never type-checks; fixed with a type-only cast). Second starter-kit tsconfig nit: `"baseUrl": "."` triggers VS Code's "deprecated, removed in TS 7.0" warning — safe to delete outright, `paths` resolves relative to tsconfig since TS 4.1 (Retab has the same line; same fix applies). Next: shop-owner validation, then decisions (name/DB/hosting/price) + schema v1. Earlier: **Arabic/RTL foundation.** App-wide `ar` locale (env only, no middleware/toggle — deliberately simpler than Retab), dynamic `lang`/`dir` in `app.blade.php`, Tajawal via Bunny fonts + the `html[dir='rtl']` `--font-sans` token override ported from Retab, faker locale `ar_JO` for future Arabic seeds. Verified end-to-end: served `/login` = `<html lang="ar" dir="rtl">`, build + 26 tests green. The starter kit's EN pages now render RTL — expected, they become the Arabic shop portal screens. Earlier same day: **Inertia v3 upgrade** (`inertia-laravel` v3.1.1 + `@inertiajs/react` 3.6.0, only break was the default-export unwrap in `app.tsx`/`ssr.jsx` — exactly as Retab predicted, zero gotchas) and **project scaffolded** (starter kit v1.0.1 on PHP 8.2, init commit `d35024e`). Next: the 7-screen HTML mockup before any schema work.
