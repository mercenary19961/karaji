<?php

return [
    /*
     | The brand's DISPLAY name per UI locale. This is the one place the
     | (still-temporary) working name lives for the UI — a rebrand is an edit
     | to these two lines (plus APP_NAME for mail/CLI and a repo rename). Never
     | hardcode the name in components; it ships via the shared `name` prop.
     */
    'name' => [
        'ar' => 'كراجي',
        'en' => 'Karaji',
    ],
];
